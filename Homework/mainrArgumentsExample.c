#include <stdlib.h>
#include <stdio.h>

int main(int argc, char *argv) {
	int nsharks;
	//should have something to check in case the user forgot to give an input
	sscanf(argv[1], "%d", &nsharks);
	//argv is precreated before your main is called given as an argument for the main
}